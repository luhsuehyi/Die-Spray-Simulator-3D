# Refine roadmap (no phone demo required)

## Done on main
- WebM demo recorder (no H.264 freeze)
- Demo UI glass / phase titles
- `renderQuality` + GPU control component
- `demoCameraPresets` (6 die-focused shots)

## Blocked until canvas restored
- Live GPU quality in the viewport
- Die-cavity camera framing in Demo Mode
- Spray mist / lighting polish in the scene

## Next after restore
1. Wire `getDemoCameraShot` + lookAt in SimulationCanvas
2. Wire quality tiers (pixel ratio, shadows, mist count, idle skip)
3. Demo narrative: full cycle titles (close → inject → open → spray → extract)
4. Stronger spray mist during spray phases only
5. Coverage % live readout in demo HUD
